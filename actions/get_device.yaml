name: get_device
runner_type: python-script
description: Retrieve details for a particular device (cube).
enabled: true
entry_point: get_device.py
parameters:
  device_uid:
    type: string
    description: UID of a device to retrieve the details for.
    required: true
